[control]
localhost ansible_connection=local

# SRV-WIN01 est ajoute dynamiquement en memoire par le playbook
# 02-wait-winrm.yml (add_host) une fois son IP confirmee joignable en
# WinRM - pas besoin de le declarer ici a l'avance.
